document.addEventListener("DOMContentLoaded", function () {
    // Código para manipular o DOM ou realizar ações após o carregamento completo do DOM

    // Por exemplo, você pode mostrar a página HTML definindo a propriedade "display" no elemento body
    document.body.style.display = "block";
});
